body {
    font-family: sans-serif;
    text-align: center;
}

.grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Cambio a fr para ser más flexible */
    grid-gap: 10px;
    margin: 20px auto;
    max-width: 350px; /* Usamos max-width para permitir flexibilidad */
    width: 100%; /* Asegura que se ajuste al contenedor */
}

.cell {
    width: 100%;
    height: 100px; /* Puedes mantenerlo fijo si lo prefieres */
    background-color: #ccc;
    border: 2px solid #999;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2em;
}

.cell.found {
    background-color: lightgreen;
}
